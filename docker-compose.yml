version: '3'

services:
  conduit-backend:
    entrypoint: /bin/sh './entrypoint-prod.sh'
    container_name: conduit_backend
    build: .
    ports:
      - "3000:3000"
    restart: always
    volumes:
      - .:/app/
