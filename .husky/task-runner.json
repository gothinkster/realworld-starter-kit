{
   "tasks": [
     {
       "name": "jb-cleanupcode",
       "command": "dotnet",
       "args": ["jb", "cleanupcode", "--include", "${staged}"],
       "include": ["**/*.cs", "**/*.vb"]
     }
   ]
}
